# Optional. Leave unset to use embedded PGLite (no Postgres required).
# DATABASE_URL=postgres://user:pass@localhost:5432/legends

# Required to create arenas. Viewing and entering stay public.
# Same value as KushMedia Node: env TOURNEY_PASSWORD, or line 1 of
# public/server/data/.tourney-password (gitignored).
# TOURNEY_PASSWORD=put-a-32-char-random-secret-here
# TOURNEY_PASSWORD_FILE=
